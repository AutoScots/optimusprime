[package]
name = "repo-zipper"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["json", "multipart", "blocking"] }
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }
